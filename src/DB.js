import firebase from "firebase/compat/app";
import "firebase/compat/database";

// for configure this use this link ans create your db
// https://console.firebase.google.com/

const config = {

    // apiKey: ...
    // authDomain: ...
    // databaseURL: ...
    // projectId: ...
    // storageBucket: ...
    // messagingSenderId: ...
    // appId: ...
    // measurementId: ...
}

const db = firebase.initializeApp(config);
export default db;